-- Drop the 'build_Link' database if it exists (if it doesn't exist, this won't cause an error).
DROP DATABASE IF EXISTS "build_Link";

-- Create a new 'build_Link' database.
CREATE DATABASE "build_Link";
